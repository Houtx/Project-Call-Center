# 管理员与坐席使用手册

> 适用代码基线：当前 `main` 源码；文档更新：2026-08-09。当前生产环境未部署。

## 使用前准备

### 管理员

- 获取 Web 管理地址和管理员账号。
- 确认要导入的客户数据有合法来源和外呼授权。
- 准备已验收的 Android 12 及以上手机，记录系统上报的 `Build.MANUFACTURER`、`Build.MODEL` 和 Android API 级别。
- 确认坐席手机可以使用公司 SIM 卡拨号，并允许企业内部 APK 安装。

### 坐席

- 获取 APK、HTTPS 服务器地址、坐席账号和密码。
- 确认用于外呼的 SIM 卡已在系统中启用；双卡设备可在 APP 的“我的”页面选择卡1、卡2或循环使用。
- 不要在通话采集未完成时清除 APP 数据、卸载 APP 或切换服务器。

## Web 管理端

### 1. 登录与导航

Web 只允许管理员登录。侧边栏包含：

- 工作台
- 客户资料
- 批次管理
- 坐席与设备
- 通话记录
- 数据报表
- 拒呼名单
- 审计日志

工作台用于查看今日外呼、接通、待呼客户、活跃坐席和近期通话。页面数据不代替定期报表导出和备份。

### 2. 先创建批次

1. 进入“批次管理”。
2. 点击“新建批次”。
3. 填写批次名称；批次编码只允许字母、数字、下划线和连字符。
4. 保存后，批次页会展示客户数、已分配数、完成数和分配进度。

批次用于组织客户来源和报表筛选，不等同于坐席团队。

### 3. 手工新增客户

1. 进入“客户资料”，点击“新增客户”。
2. 填写客户名称和 11 位手机号。
3. 手机号输入完并离开输入框后，系统会尝试填入运营商、省份和城市。
4. 如果号段结果不准确，可以手工改写；管理员输入优先。
5. 可选择批次，填写标签和备注，然后保存。

如果号码已存在或已在拒呼名单，服务端会拒绝新增。已建客户的手机号不可修改。

### 4. 批量导入客户

CSV 可以从 [examples/customers-template.csv](examples/customers-template.csv) 开始准备；Excel 请在导入弹窗中点击“下载 Excel 模板”。文件必须且只能包含两列：

```text
姓名,手机号
```

操作步骤：

1. 在“客户资料”点击“导入”。
2. 必须先选择本次导入的批次；文件内不填批次。
3. 选择 `.csv` 或 `.xlsx` 文件，文件不得超过 20 MiB，单次不得超过 100,000 行。
4. 等待预览，检查批次、新增、重复、无效和拒呼数量以及前 100 行样本。
5. 重复策略选择“跳过”或“更新姓名、批次及自动识别资料”。
6. 确认后提交。完成消息会显示新增、更新和跳过数量。

省份、城市和运营商会根据手机号自动识别。导入前应抽样复核，特别是携号转网号码。系统不会根据文件内容自动创建批次。

### 5. 分配、回收和改派

1. 使用客户名称/号码、批次、坐席和分配状态筛选客户。
2. 勾选需要处理的客户，点击“分配坐席”。
3. 选择目标坐席。如客户已分配，操作将成为改派，旧归属历史保留。
4. 需要撤回任务时，勾选客户并点击“回收任务”。已有通话历史不受影响。
5. 点击单个客户的“分配历史”可查看归属变更记录。

客户页顶部的“分配”下拉菜单还支持批量分配：

- “根据当前搜索结果分配”沿用当前搜索、批次、坐席、状态和分配状态筛选。
- “全部可分配数据”忽略当前筛选，只处理仍为可用或已分配状态的客户。
- 目标坐席允许多选，只需输入一个总分配数量。总量不能少于所选坐席数，也不能超过可分配数。
- 弹窗会显示每名坐席的预计分配数，并汇总匹配数、可分配数、本次计划总量、本次不处理数量和将跳过数量。
- 确认时服务端会重新计算并平均分配；不能整除时，按弹窗中的坐席顺序依次多分 1 位。已完成、归档、拒呼和其他不可分配状态不会被强行分配。
- 批量操作按服务端分块执行，超过单次手工选择上限时不需要把全部客户加载到浏览器。

需要再次尝试未接通客户时，在“分配状态”选择“已呼未接通”并查询。该筛选只判断最新分配下的最新一通结果，后来已经接通的客户不会出现。勾选结果后点击“重新派发”可交给一名坐席；也可使用“分配 > 根据当前搜索结果分配”，选择多名坐席和一个总数量后平均重新派发。原分配和外呼历史保留，新分配从当前后台配置的最大外呼次数重新计数。重新派发成功后，客户会离开该筛选，直到新分配再次产生未接通结果。

已归档、已拒呼或不满足业务状态的客户不应进行分配。

### 6. 创建坐席与设备登录

1. 进入“坐席与设备”，创建坐席姓名、登录账号和不少于 10 位的初始密码。
2. 在“兼容策略”添加已完成真机验收的品牌、型号和 Android API 精确组合。
3. 把服务器地址、坐席账号和初始密码通过受控渠道交给坐席，不再生成设备激活码。
4. 坐席在 APP 登录成功后，当前手机自动成为唯一活跃设备。
5. 同一账号在新手机登录会立即撤销旧手机会话，不要在旧手机尚有待采集通话时换机。管理员仍可在 Web 主动撤销当前设备。

停用坐席后该账号无法登录。当前 Web 页面未暴露坐席密码重置按钮；后端已提供受管理员保护的重置接口，应由受权运维人员执行并留存审计。

### 7. APP 版本与机型策略

“兼容策略”包含：

- 最低版本号：低于此 `versionCode` 的 APP 被服务端拒绝外呼。
- 最新版本号：不得小于最低版本号。
- APK 下载地址：必须是完整 HTTPS URL。
- 强制升级：开启后，低于“最新版本号”的 APP 无法登录或继续外呼；已达最新版本的设备仍可使用。
- 单个客户最大外呼次数：可设置 1-10 次，默认 2 次；降低上限后，已经达到新上限且没有正在采集通话的任务会立即完成。
- 设备兼容校验：默认开启，要求品牌、型号和 Android API 命中机型白名单。关闭后跳过这三项白名单检查，允许未列入白名单的 Android 12+ 设备登录；APP 最低版本、设备状态和拨号/通话记录权限仍然必须满足。
- 机型白名单：只有启用的精确品牌+型号+Android API 组合可登录和外呼。

GitHub Release 启动门禁与服务端版本策略是两层校验。发布新 APK 后要同步检查 Release 清单和 Web 最低/最新版本号。开启“强制升级”前，必须确保最新版 APK 已可下载。

### 8. 拒呼名单

1. 进入“拒呼名单”，点击“添加号码”。
2. 填写手机号和拒呼原因。
3. 保存后，系统会立即撤回该号码的未完成任务。
4. 移出拒呼名单后，原撤回任务不会自动恢复，需要时由管理员重新分配。

拒呼名单中的完整号码不会出现在列表、日志或审计说明里。

### 9. 通话、报表与审计

- “通话记录”按日期、坐席、批次和结果筛选，可查看单通尝试 ID、客户、坐席、发起/结束时间、时长和回传时间。
- “数据报表”展示外呼次数、去重客户、已接通、未接通、采集中、未知、数据完整率、接通率、总时长和平均时长。
- “审计日志”用于检索管理操作、完整号码查看、导入导出、分配、拒呼和设备操作。
- 客户和通话导出会生成审计事件。导出文件包含敏感信息，应在受控位置保存和传输。

### 10. 归档与合规删除

- “归档”把客户移出日常分配流程，不删除历史。
- 只有已归档客户才能执行“依法删除个人数据”。
- 执行删除时必须填写原因；匿名化统计和审计记录保留。

## Android 坐席 APP

### 1. 首次安装与更新门禁

1. 从本组织指定的 Release 页安装已签名 APK；公开源码仓库不应存放业务 APK 或客户数据。
2. APP 每次启动都先请求 GitHub `update-manifest.json`。
3. 新版本存在时，APP 下载 APK，校验大小、SHA-256、包名、版本号和签名，然后打开系统安装器。
4. 如系统要求“允许此来源安装应用”，授权后回到 APP 继续安装。
5. 更新检查失败、下载失败或取消安装时，APP 保持锁定，不能进入外呼页。

Android 不允许普通应用静默安装 APK，因此“强制更新”表示不更新就无法使用，但仍需要用户在系统安装器中确认。

### 2. 服务器与登录

1. 在登录页填写服务器域名或完整地址。Release 版只允许 HTTPS。
2. APP 会把地址规范化到 `/api/v1/`；地址不得包含用户名、密码、查询参数或页面锚点。
3. 登录页会显示本机实际提交的品牌、型号和 Android API；管理员应按该标识精确配置机型白名单。
4. 填写坐席账号和密码，点击“连接并登录”。
5. 服务端检查坐席、机型白名单、Android API 和 APP 版本，然后自动绑定当前手机。
6. 同一坐席账号只允许一台活跃手机。新手机登录后，旧手机的访问与刷新令牌立即失效，旧 APP 在下一次请求或最迟约 15 秒内回到登录页。
7. 密码不在 APP 中持久化；APP 仅保存受保护的登录令牌和设备绑定状态。

启动时已保存的服务器需要重新验证。网络暂时不可达时应先重试，不要立即清空 APP 数据。

### 3. 权限

APP 需要：

- `CALL_PHONE`：交给系统拨号器发起通话。
- `READ_CALL_LOG`：在外呼后匹配系统通话记录。
- `READ_PHONE_STATE`：识别当前可拨号的卡槽并将外呼交给所选 SIM。

任一权限未授予时，服务端不会下发拨号号码。APP 不读取联系人，不上传非本 APP 发起的个人通话。

### 4. 设置拨号 SIM

- 进入“我的”页面，在“拨号 SIM 卡”中查看系统识别到的卡槽和运营商名称。
- 双卡设备可选择“卡1”“卡2”或“循环”；选择结果保存在本机。
- 循环模式按卡1、卡2依次发起成功提交给系统的外呼。
- 只有一张可拨号 SIM 时自动进入单卡模式并使用该卡，不受原双卡设置影响；恢复双卡后继续使用原设置。
- 无可拨号 SIM 或电话状态权限被撤销时，任务页会禁用拨号。

### 5. 执行外呼

1. 进入“任务”页，必要时点击右上角刷新。
2. 任务卡显示客户、脱敏号码、地区、运营商、批次和已尝试次数。
3. “查看号码”会向服务端申请完整号码并记录审计。
4. 点击“拨号”后，服务端重新校验任务，APP 创建待采集尝试后打开系统电话。
5. 通话结束后回到 APP。APP 在前台继续轮询 CallLog，并通过 WorkManager 补采。
6. “记录”页显示本人通话状态和时长；尚未匹配时显示“待核验”。

同一时间只允许一条待采集通话。未接通后该任务会移到列表末尾，并需等待 30 分钟才能重拨；默认最多外呼 2 次，实际次数以管理员后台当前配置为准。

### 6. 切换服务器与换机

- 在“我的”页点击“切换服务器”回到配置页。
- 切换到不同服务器会清理旧令牌、设备绑定和本地业务缓存。
- 如有待采集通话，APP 会阻止切服；应恢复原服务器连接并完成回传。
- 换机时直接在新手机登录，旧手机会自动下线；管理员无需提前撤销。
- 换机前必须确认旧手机没有待采集通话，否则该通话可能因旧令牌失效而在 24 小时后转为“未知”。
- APP 不采集 IMEI。

## 常见问题

| 现象 | 首先检查 | 处理 |
| --- | --- | --- |
| APP 一直停在“正在检查更新” | 手机是否可访问 GitHub Release | 恢复网络后点击重试；不要绕过更新门禁 |
| 无法安装更新 | “允许此来源安装”权限、存储空间 | 授权后返回 APP，重新打开系统安装器 |
| 服务器地址无法验证 | HTTPS、域名、证书、`/api/v1/health` | 使用域名或完整 HTTPS 地址，不要携带查询参数 |
| 坐席无法登录 | 账号密码、坐席状态、机型白名单和版本策略 | 核对账号和 `MANUFACTURER/MODEL/API`，必要时由管理员关闭兼容校验 |
| 提示账号在其他手机登录 | 是否有人用同一坐席账号 | 确认账号使用人；如非本人操作，立即联系管理员重置密码 |
| APP 没有任务 | 坐席是否启用、客户是否分配给该坐席 | Web 重新分配，APP 回到前台或手工刷新 |
| 拨号按钮不可用 | 三项电话权限、可拨号 SIM、待采集通话、版本策略 | 授予权限，启用 SIM，等待上一通完成采集，必要时更新 APP |
| 提示 30 分钟内不能重拨 | 上一次发起时间 | 等待服务端返回的可重试时间 |
| 通话长时间“待核验” | 通话记录权限、系统是否生成 CallLog | 保持 APP 权限，重新打开 APP 并联网；24 小时后仍无法匹配则转为未知 |
| 无法切换服务器 | 是否有待采集通话 | 恢复原服务器并完成回传，不要清空 APP 数据 |
| 报表未立即变化 | 该通是否仍在采集 | 等待 APP 回传并刷新；迟到结果会自动更正 |

## 安全提醒

- 不在工单、群聊或截图中传播完整客户号码。
- 不共享账号，不在公共设备上保存管理员密码。
- 坐席账号不共享；发现异常顶号应立即重置密码并查看审计日志。
- 导出 CSV 与生产备份分开管理；CSV 不是可恢复整个系统的备份。
- 安卓系统拨号器和最近通话中仍会显示完整号码，BYOD 场景不能从技术上完全隐藏该信息。

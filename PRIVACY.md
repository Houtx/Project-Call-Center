# 隐私说明

> 本文说明开源代码的默认数据行为。实际部署者仍需根据所在地法律、号码来源、员工设备政策和自身配置提供正式隐私告知。

## 两种使用模式

Android APP 提供两种相互隔离的模式：

- 在线 CRM 模式会连接用户配置的业务服务器，下载当前坐席任务，并回传由 APP 创建的外呼尝试及匹配结果。
- 本机独立模式不登录或同步 CRM。导入的姓名、号码、待呼状态和通话明细只保存在当前手机的独立数据库。

切换模式不会复制客户或通话数据。APP 的版本更新检查独立于业务模式，仍会访问构建时配置的 HTTPS 更新源。

## Android 权限

APP 使用以下系统权限：

| 权限 | 用途 |
| --- | --- |
| `CALL_PHONE` | 把用户明确选择的号码交给系统电话服务拨出 |
| `READ_CALL_LOG` | 匹配 APP 已创建外呼尝试之后生成的系统外呼记录 |
| `READ_PHONE_STATE` | 识别可拨号 SIM，并执行卡 1、卡 2 或循环策略 |
| `RECORD_AUDIO` | 仅在管理员为在线坐席启用可选录音且设备支持时使用 |
| `INTERNET` | 在线 CRM、版本更新和用户选择开启的匿名统计 |

APP 不读取通讯录，不批量上传系统通话记录，也不应上传其他应用或员工个人发起的电话。Android/OEM 的 CallLog 能力存在局限，正式使用前必须对每个机型做真机验收。

## 数据保护与可见边界

- 在线服务器使用加密号码和独立 HMAC 索引；完整号码访问、导出及敏感管理操作应进入审计。
- 本机独立模式使用 Android Keystore 保护的 AES-GCM 密文保存号码和姓名，并使用本机随机 HMAC 密钥去重。
- 本机独立模式需要本地密码，连续输错会限流，进入后台 1 分钟后自动锁定。
- 表格预览的临时副本位于 APP 私有缓存，只在导入会话期间保留，并在完成、失败、锁定、切换模式或下次进程初始化时清理。
- 系统拨号器、系统最近通话和运营商侧记录不受 APP 加密或截图保护控制，仍可能显示完整号码。
- 卸载 APP、清除数据、遗失手机或忘记本地密码可能使本机独立数据永久丢失；当前不提供云端恢复。

## 可选匿名使用统计

开源构建默认不配置统计接收地址，因此不显示开关，也不会发送任何使用统计。部署者只有在构建时提供 HTTPS `CALL_CENTER_TELEMETRY_URL` 后才可启用该能力，且用户必须主动同意。

统计最多每天发送一次小型聚合，使用与在线安装 ID、设备绑定和账号无关的随机标识。允许字段限于 APP 版本、Android 大版本、在线/独立模式、语言与时区，以及按日汇总的外呼数、结果数和接通时长。禁止发送：

- 手机号、姓名、备注、标签、文件名和客户 ID；
- SIM 标识、运营商、IMEI、通讯录或完整设备指纹；
- 业务服务器地址、账号、坐席 ID、通话逐条时间或精确位置；
- 原始导入文件、CallLog 原文、录音或崩溃时的敏感内存内容。

统计失败不得阻止登录、独立模式、拨号或记录采集，不建立无界离线队列。用户关闭开关后应立即停止后续发送。官方接收端仅保留带密钥的 IP HMAC 和脱敏网段，活跃明细默认保留 30 天；国家来自可信代理头，不调用第三方 IP 查询服务。部署者应公开接收方、用途、保留期和删除渠道，不得尝试还原或关联具体员工身份。

## 公开仓库与问题反馈

不要在 GitHub Issue、Pull Request、截图、测试文件或日志中提交真实号码、姓名、服务器地址、账号、密钥、APK 签名材料、客户表格或生产备份。安全问题请按 [SECURITY.md](SECURITY.md) 的私下报告渠道处理。

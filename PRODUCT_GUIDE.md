# 产品与业务规则

> 适用代码基线：当前 `main` 源码；文档更新：2026-08-19。

## 产品目标

Project Call Center 用于管理员将客户号码分配给坐席，由 Android 手机使用本机 SIM 卡外呼，并将可获取的系统通话记录回传到 Web 管理端。首版优先保证任务归属、拒呼拦截、通话追踪和数据完整性，不建设复杂销售漏斗。

## 用户与边界

| 角色 | 使用端 | 主要权限 |
| --- | --- | --- |
| 管理员 | Web | 客户、批次、分配、坐席、设备、机型白名单、拒呼、通话、报表、导出和审计 |
| 坐席 | Android APP | 在线同步本人任务，或在本机独立模式导入号码；发起外呼并查看本人历史、统计和设备状态 |

首版是单公司系统，没有主管角色、多团队数据隔离或坐席 Web 后台。

Android APP 有两条相互隔离的数据路径：在线 CRM 模式以 API 为业务事实来源；本机独立模式不登录 CRM，客户、队列和通话明细只保存在当前手机。两种模式可以切换，但不会互相复制任务或记录。APP 启动更新检查和用户明确同意的匿名使用统计仍可能访问互联网。

## 核心流程

1. 管理员创建批次，再手工新增或选择批次后从两列 CSV/XLSX 导入客户。
2. 系统对手机号归一化并执行全局去重；省份、城市和运营商缺失时，用内置离线号段库尝试补全。
3. 导入先显示新增、重复、无效和拒呼数量，管理员选择跳过重复或更新已有客户的非号码字段。
4. 管理员可勾选客户分配单个坐席，也可按当前筛选或全部可分配范围选择多名坐席、输入总数并平均分配；还可按最新外呼结果筛出未接通客户并重新派发。一个客户同时只能有一个活跃分配，改派、重试派发和回收均保留历史。
5. Android APP 启动、回到前台或手工刷新时同步本人任务，任务列表默认只显示脱敏号码。
6. 坐席使用账号密码登录，服务端自动绑定当前手机。同一账号只允许一台活跃设备，新手机登录会立即使旧手机会话失效。
7. 坐席点击拨号时，服务端再次校验分配、拒呼、设备、权限、APP 版本、次数和 30 分钟间隔，通过后才返回短时有效的完整号码。
8. APP 先保存外呼尝试，再按坐席设置使用卡1、卡2或循环模式调用系统拨号器；只有一张可拨号 SIM 时自动使用该卡。通话返回后，APP 仅匹配与该 `attemptId` 对应的新增外呼 CallLog。
9. 连接成功立即完成任务；未接通后任务移到待呼列表末尾，满 30 分钟后可重试；达到管理员配置的最大外呼次数后关闭任务，默认 2 次，可设置 1-10 次。

### 本机独立模式

1. 坐席在启动页选择“只在本机离线使用”，首次进入需创建至少 6 位的本地密码。
2. 可选择 `.xlsx`、`.csv`、`.tsv`，从多个工作表和任意列中指定手机号列与可选姓名列；也可粘贴由换行、Tab、逗号或分号分隔的号码。
3. 导入前展示列样本和手机号有效率。系统归一化中国大陆手机号并在本机去重，单个文件最多 25 MiB、100,000 行、256 列。
4. APP 在本机创建外呼尝试后调用系统 SIM 拨号，并用同样的 CallLog 规则生成已接通、未接通或未知记录。未接通移到队尾，默认最多 2 次，可在 1-10 次之间调整。
5. 本机可筛选待呼、未接通和全部数据，查看今日/近 7 天/全部统计，并清理 10、15 或 30 天前的已完成联系人及关联记录。
6. 客户号码和姓名使用 Android Keystore 保护的 AES-GCM 密文保存在独立 Room 数据库；切换在线服务器、在线退出或 CRM 清缓存不会删除或上传这些数据。
7. 退到后台 1 分钟后自动锁定；系统拨号器和系统最近通话仍会显示完整号码。忘记本地密码后无法找回，只能清除 APP 数据。

工作台会按 Asia/Shanghai 的自然日汇总所有产生过外呼记录的坐席，显示外呼次数、去重客户、接通与未接通数量、采集中与未知数量、接通率以及平均、最长和总通话时长。接通率和时长口径与数据报表保持一致。

## 客户与号码规则

- 当前 Web 入口接受 11 位中国大陆手机号。服务端使用标准化值计算唯一 HMAC，全系统一个号码只有一个客户。
- 手工新增时，手机号输入框失去焦点后会尝试自动填写省份、城市和运营商；保存时后端会再执行同样的补全。
- 导入文件不接受地区或运营商列；系统会统一根据手机号自动识别，导入后管理员仍可在客户资料中复核和修正。
- 号段归属是运营数据，不是实时运营商证明。携号转网、新号段或号段库滞后可能导致不准确，管理员可以手工修正。
- 号码本文使用 AES-256-GCM 加密存储，列表、日志和审计只显示脱敏值。查看完整号码会产生审计记录。
- 客户号码不允许原地修改。需要更换号码时，应新建客户，以保持去重和通话历史连续性。

## 导入规则

| 项目 | 规则 |
| --- | --- |
| 格式 | `.csv` 或 `.xlsx` |
| 文件大小 | 最大 20 MiB |
| 单次行数 | 最大 100,000 行；必须先在目标环境完成内存压测，低内存主机不得直接执行该上限 |
| 必填列 | 姓名、手机号；只允许这两列 |
| 批次 | 上传前由管理员必选，文件中不携带 |
| Excel 模板 | 从导入弹窗下载空模板 |
| 重复默认 | 跳过 |
| 重复更新 | 更新姓名、所选批次和号段自动识别资料，不覆盖历史通话或分配记录 |
| 原文件 | 仅在请求内存中解析，不作为原文件长期保存 |

导入明细和加密号码会保存在 PostgreSQL，用于预览、提交结果和审计。

## 分配状态

| 状态 | 含义 | 后续动作 |
| --- | --- | --- |
| 未分配 | 没有活跃坐席任务 | 可手工分配 |
| 待外呼 | 已分配且任务未完成 | 坐席 APP 可同步 |
| 已呼未接通 | 最新分配下的最新一通结果为未接通 | 可重新派发给一名或多名坐席，新分配重新计算外呼次数 |
| 已完成 | 已接通，或达到当前最大外呼次数 | 不再作为待呼任务 |
| 已撤回 | 管理员回收、改派或拒呼规则终止旧任务 | 如允许，需管理员重新分配 |

筛选只判断最新分配下的最新外呼结果，因此后来已接通的客户不会被误选；重新派发后，在新分配产生未接通结果前不会继续出现在该筛选中。分配和改派使用数据库唯一约束与串行事务防止并发重复归属。

## 通话状态

| 状态 | 来源 | 报表口径 |
| --- | --- | --- |
| 采集中 `COLLECTING` | 已发起外呼，APP 尚未匹配到 CallLog | 计入外呼次数，不进入接通率分母 |
| 已接通 `CONNECTED` | CallLog `duration > 0` | 计入接通数、时长和接通率 |
| 未接通 `NOT_CONNECTED` | CallLog `duration = 0` | 计入未接通数和接通率分母 |
| 未知 `UNKNOWN` | 24 小时内无法获得可匹配 CallLog | 计入外呼次数，不进入接通率分母，降低数据完整率 |

Android 系统 CallLog 不能稳定区分忙线、关机、拒接和无人接听，也不提供计费级接通时刻。系统数据用于运营管理，不作为计费、仲裁或争议证据。

## 报表口径

- 外呼次数：筛选范围内所有已创建的外呼尝试。
- 去重客户数：至少有一次外呼尝试的不同客户数。
- 接通率：`CONNECTED / (CONNECTED + NOT_CONNECTED)`。
- 数据完整率：有明确 CallLog 结果的外呼数除以全部外呼数。
- 总时长：已接通通话的 `durationSeconds` 合计。
- 平均时长：已接通通话总时长除以已接通数。
- 迟到 CallLog 可以把 `UNKNOWN` 更正为已接通或未接通，报表随之重新计算。

## 拒呼与合规删除

- 拒呼号码在导入、分配、查看和拨号环节拦截。
- 号码加入拒呼名单后，未完成分配立即撤回；移出拒呼名单不会自动恢复旧任务。
- 合规删除前必须先归档客户并结束活跃分配。
- 合规删除会清除客户个人字段和加密号码标识，但保留匿名化分配、通话统计和带原因的审计事件。

## 明确不包含

- 排班、考勤、作息管理和主管审批。
- 自动连续拨号、SIP/VoIP、呼入、语音识别和 AI 话术。
- 人工联系结果、销售漏斗、商机和订单管理。
- 智能分配、多团队租户隔离和后台实时推送。
- 普通 BYOD 设备上的通话录音保证、计费级证明或运营商通话详单对账；可选录音只面向经过企业/OEM 授权并完成真机验收的设备。
- 旧系统历史数据自动迁移。

## 当前规模说明

- 系统不设置坐席数量的业务上限；实际可承载数量以部署服务器资源和压测结果为准。
- 日外呼量不超过 10,000 次。
- Android 12/API 31 及以上，企业内部安装，支持一张或两张可拨号 SIM，不替换默认电话应用。
- 每个坐席同时只允许一台活跃设备；新手机登录自动顶掉旧手机，无需激活码或管理员预先撤销。
- 业务数据默认长期保留，通过归档、合规删除和离站备份管理生命周期。

本机独立模式没有坐席账号、服务器分配、拒呼名单同步或跨设备备份。卸载 APP、清除应用数据、遗失手机或忘记本地密码都可能造成其本机数据不可恢复。

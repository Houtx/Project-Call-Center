# 已知限制与上线前待办

> 适用代码基线：当前 `main` 源码。

本文档只记录当前代码和已完成验证能支持的事实。修复某项后，应在同一个变更中更新或移除对应记录。

## 阻断生产上线

| 问题 | 影响 | 处理门槛 |
| --- | --- | --- |
| 目标环境容量尚未验证 | 代码中的硬上限不能证明目标共享主机有足够余量 | 先在目标环境执行隔离压测；默认运行容器上限约 1.1 GiB，上线前至少保留 1.5 GiB `MemAvailable` |
| 没有已发布的 `linux/amd64` API/Web 镜像 | 开发 Mac 的 `arm64` 镜像不能用于 `x86_64` 生产主机 | 在 CI/隔离构建机发布不可变 tag + digest |
| 生产离站备份尚未配置 | 没有真实 S3、`age` 收件人、定时任务和恢复演练 | 真实客户数据进入前完成全套备份/恢复验收 |
| 生产 API/Web 域名尚未配置 | Android 在线模式仍需要稳定的 HTTPS CRM API；当前 `call.haoyunqiankun.com` 仅提供 APK 更新 | 完成目标服务器容量验证后，使用独立业务域名部署 API/Web，不能把更新静态站误当成 CRM API |

## 性能与运维限制

### 导入是同步内存流程

CSV/XLSX/XLS 仍由 API 同步处理，代码硬上限为 100,000 行，生产 Compose 默认限制为 10,000 行。CSV 解析器和 XLSX 流式预检会在超过配置行数时提前停止，XLSX 预检还限制 ZIP 条目数和关键 XML 的实际解压大小；旧版 XLS 在 20 MiB 文件上限内使用二进制工作簿解析器。通过预检后，上传文件 Buffer、共享字符串以及限制内的待导入行仍占用 API 内存。历史全量解析 10 万行时 API cgroup 峰值约 1.42 GiB、PostgreSQL 峰值约 576 MiB，因此低配生产配置不能直接调回 10 万行。客户和通话 CSV 导出已经改为每批 500 条的流式输出，不再一次性构造 10 万行 Buffer。

当前约束：

- 不在小内存共享主机提高 `IMPORT_MAX_ROWS`。
- 导入压测只用独立数据库。
- 如 10,000 行仍超时或触发 OOM，继续降低上限；不能只通过调大 Nginx 超时掩盖内存问题。
- 批量分配会加载候选 ID，生产默认限制为单次分配 10,000 条、扫描 100,000 条；更大范围必须按批次拆分。

### 录音与长期业务数据会持续占盘

录音使用本机加密文件存储，保留期到达后由后台任务删除。客户、通话和审计按业务要求长期保留，不会自动清理。启用录音前必须按日呼叫量计算容量并设置磁盘/inode 告警；低配共享盘优先关闭录音，或先实现并验证独立对象存储。

录音上传和管理员播放当前会在 API 内存中缓冲单个文件，单文件上限 25 MB。低配服务器不要并发上传或播放大量长录音；正式启用录音前应压测这一链路，后续高并发场景需改为真正的流式加解密与对象存储。

### 报表用于运营而非计费

Android CallLog 不能可靠区分忙线、关机、拒接和无人接听，不提供计费级接通时刻。`duration > 0` 只是本系统的运营口径。

## Android 限制

| 限制 | 当前行为 |
| --- | --- |
| 真机验收尚未完成 | 自动化测试不能证明各品牌 CallLog 行为；正式开放前必须 5-10 人试运行至少 7 天 |
| 更新依赖生产更新域名 | APP 每次进程冷启动请求 `call.haoyunqiankun.com`；普通后台恢复不会重复检查。更新源不可达、清单无效或 APK 校验失败时会锁定业务，必须监控更新域名并保留 GitHub/人工分发 APK 的应急流程 |
| 独立模式没有云端备份 | 客户和通话数据只在当前手机，卸载、清除 APP 数据、手机遗失或忘记密码都可能导致数据不可恢复 |
| 独立模式不继承 CRM 控制 | 不同步拒呼名单、坐席权限、批次、管理员分配或跨设备历史；导入和外呼合规由设备使用者负责 |
| Android 无法静默安装 | 发现新版后锁定业务，但用户仍需在系统安装器确认 |
| 被顶设备的待采集通话 | 新手机登录会立即使旧令牌失效；若旧机仍有待匹配 CallLog，旧机无法继续回传，24 小时后服务端会转为未知 |
| 双卡路由依赖厂商实现 | APP 可指定卡1、卡2或循环并在单卡时自动回退；鸿蒙卓易通等不公开订阅/电话账户映射的环境会改用系统托管路由，此时由系统默认卡拨号，APP 无法保证固定卡或循环。每个品牌/型号/系统版本仍须真机验证拨号和 CallLog 采集 |
| 系统界面会显示完整号码 | 系统拨号器和最近通话属于 Android/BYOD 边界，APP 不能隐藏 |
| 同号码个人电话存在归因边界 | APP 只查询外呼发起后两分钟内新增的同号码 CallLog；若员工在取消 APP 拨号后立刻用同一 SIM 手工拨打同一号码，普通应用仍无法从 CallLog 绝对区分，机型验收和操作规范必须覆盖该场景 |
| 切服需先清空待采集通话 | 本地有待匹配尝试时会阻止切换服务器，防止通话数据丢失 |

## Web 管理功能缺口

- Web 已有坐席创建、启停和设备撤销，但尚未暴露坐席密码重置界面。后端有受管理员权限保护的重置接口。
- Web 尚未提供管理员个人密码修改界面；后端已有 `POST /auth/change-password`。
- 首版不包含作息/排班/考勤、人工联系结果、无人值守批量拨号、多团队和智能分配。自动拨号必须由坐席在任务页主动开启，并在每通返回 APP、核验结果后继续。可选录音不保证在普通 Android/BYOD 设备可用。

## 号段归属限制

省份、城市和运营商来自离线号段库。携号转网、新号段和数据库滞后会导致偏差。管理员明确输入的值优先，查询失败不阻止合法号码保存。

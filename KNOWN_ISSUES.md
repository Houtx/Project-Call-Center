# 已知限制与上线前待办

> 适用代码基线：当前 `main` 源码。

本文档只记录当前代码和已完成验证能支持的事实。修复某项后，应在同一个变更中更新或移除对应记录。

## 阻断生产上线

| 问题 | 影响 | 处理门槛 |
| --- | --- | --- |
| 目标环境容量尚未验证 | 10 万行导入可能触发较高 API 与数据库内存峰值 | 先在目标环境执行隔离压测；建议至少 2 vCPU、4 GiB 内存且上线前有 3 GiB 可用内存 |
| 没有已发布的 `linux/amd64` API/Web 镜像 | 开发 Mac 的 `arm64` 镜像不能用于 `x86_64` 生产主机 | 在 CI/隔离构建机发布不可变 tag + digest |
| Compose 无 CPU/内存/PID/日志上限 | 峰值可能拖慢整台共享主机 | 按压测结果配置资源上限与报警 |
| Worker 无业务健康指标 | 进程存活不代表 24 小时对账和 housekeeping 正常 | 增加最近循环时间、错误和 backlog 监控 |
| 生产离站备份尚未配置 | 没有真实 S3、`age` 收件人、定时任务和恢复演练 | 真实客户数据进入前完成全套备份/恢复验收 |
| 生产域名与 APK 发布源尚未配置 | Android Release 只允许 HTTPS，启动更新检查也依赖公开发布源 | 配置自己的 DNS、证书和独立 APK Release 仓库 |

## 性能与运维限制

### 导入是同步内存流程

CSV/XLSX 在 API 内存中解析，最大允许 100,000 行。实测中 API cgroup 峰值约 1.42 GiB，PostgreSQL 峰值约 576 MiB。当前没有流式解析、后台队列或分片提交。

在优化前：

- 不在小内存共享主机执行大导入。
- 导入压测只用独立数据库。
- 生产 Nginx 60 秒读超时对 10 万行导入余量不足，不应只通过无限调大超时掩盖内存问题。

### 报表用于运营而非计费

Android CallLog 不能可靠区分忙线、关机、拒接和无人接听，不提供计费级接通时刻。`duration > 0` 只是本系统的运营口径。

## Android 限制

| 限制 | 当前行为 |
| --- | --- |
| 真机验收尚未完成 | 自动化测试不能证明各品牌 CallLog 行为；正式开放前必须 5-10 人试运行至少 7 天 |
| 更新依赖 GitHub | APP 每次启动要先请求公开 Release 清单；GitHub 或网络不可达时无法进入业务 |
| Android 无法静默安装 | 发现新版后锁定业务，但用户仍需在系统安装器确认 |
| 被顶设备的待采集通话 | 新手机登录会立即使旧令牌失效；若旧机仍有待匹配 CallLog，旧机无法继续回传，24 小时后服务端会转为未知 |
| 双卡路由依赖厂商实现 | APP 可指定卡1、卡2或循环并在单卡时自动回退，但每个品牌/型号/系统版本仍须验证 `PhoneAccountHandle` 是否正确路由到对应卡槽 |
| 系统界面会显示完整号码 | 系统拨号器和最近通话属于 Android/BYOD 边界，APP 不能隐藏 |
| 切服需先清空待采集通话 | 本地有待匹配尝试时会阻止切换服务器，防止通话数据丢失 |

## Web 管理功能缺口

- Web 已有坐席创建、启停和设备撤销，但尚未暴露坐席密码重置界面。后端有受管理员权限保护的重置接口。
- Web 尚未提供管理员个人密码修改界面；后端已有 `POST /auth/change-password`。
- 首版不包含作息/排班/考勤、人工联系结果、自动连拨、录音、多团队和智能分配。

## 号段归属限制

省份、城市和运营商来自离线号段库。携号转网、新号段和数据库滞后会导致偏差。管理员明确输入的值优先，查询失败不阻止合法号码保存。
